# Stripe API Configuration
# Get your keys from: https://dashboard.stripe.com/apikeys

# Secret key for authenticating with Stripe API (server-side only, NEVER expose to frontend)
STRIPE_SECRET_KEY=sk_test_your_key_here

# Webhook signing secret for verifying webhook authenticity
# Get this from: https://dashboard.stripe.com/webhooks
# Or use Stripe CLI: stripe listen --print-secret
STRIPE_WEBHOOK_SECRET=whsec_your_secret_here

# Note: Use test keys (sk_test_) for development and live keys (sk_live_) for production
